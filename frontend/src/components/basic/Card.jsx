import React from 'react'
import {DateBlock} from '../'

const Card = ({title="Title"}) => {

    return (
        <div>
            <DateBlock variant='cardDate'/>
        </div>
    )
}

export default Card
